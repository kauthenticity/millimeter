import React from "react";
import styled from "styled-components";

const Logout = () => {
  return <LogoutContainer></LogoutContainer>;
};

export default Logout;

const LogoutContainer = styled.div``;
